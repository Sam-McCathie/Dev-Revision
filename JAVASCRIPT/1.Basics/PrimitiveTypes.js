//Primative/ Value types;
// - String
let fName = "Sam"; // String literal

// - Number
let age = 25; // Number literal if the number had " " it would be a string.

// - Boolean
let isHandsome = true; // Boolean Literal (can be true or false)

// - undefined
let fine; // Will be undefined can be manually set as let fine = undefined;

// - null
let something = null; // used when you want to clear the value of a variable.

// note -> JavaScript is a dynamic language and a variable can be changed.
// Other languages cannot and they are called static languages.

// type -> typeof variableName; into console to find out the type of variable.
